{
  "buildCommand": "pnpm turbo build --filter=@typedframe/demo",
  "installCommand": "pnpm install",
  "outputDirectory": "apps/demo/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
